var width = window.innerWidth;
var hight = window.innerHeight;

function GetRes() {
    console.log('The resolution is: Width: ' + width + ", Height: " + hight)
}

// This is really useless, but it sometimes is needed.